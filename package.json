{
  "name": "metawritingecology-site",
  "description": "Public surface website for Meta-Writing Ecology.",
  "cloudflare": {
    "label": "Meta-Writing Ecology Site",
    "products": [
      "Workers"
    ],
    "categories": [],
    "publish": true
  },
  "dependencies": {
    "@astrojs/cloudflare": "12.6.12",
    "@astrojs/mdx": "4.3.13",
    "@astrojs/rss": "4.0.14",
    "@astrojs/sitemap": "3.6.1",
    "astro": "5.16.9",
    "typescript": "5.9.3"
  },
  "devDependencies": {
    "wrangler": "4.88.0"
  },
  "engines": {
    "node": ">=22"
  },
  "private": true,
  "scripts": {
    "astro": "astro",
    "build": "astro build",
    "cf-typegen": "wrangler types",
    "check": "astro build && tsc && wrangler deploy --dry-run && pnpm run test:contracts && pnpm run verify:public-surface-map",
    "deploy": "wrangler deploy",
    "dev": "astro dev",
    "preview": "astro build && wrangler dev",
    "test:contracts": "node --test tests/public-surface-authority-map/contracts.test.ts",
    "verify:public-surface-map": "node scripts/verify-public-surface-map-build.mjs"
  },
  "type": "module"
}
